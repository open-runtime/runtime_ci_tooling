## Version Bump Analysis

**Decision**: patch

**Key Changes**:
- **Refactor**: Replaced manual CLI argument parsing with `build_cli` generated argument parsers for improved maintainability and consistency.
- **Dependencies**: Added `args`, `build_cli`, and `build_runner` dependencies.
- **CI Templates**: Updates to `ci.template.yaml` and `release.template.yaml`.

**Breaking Changes**:
- None. The CLI interface (arguments/flags) remains compatible with the previous version.

**References**:
- Refactor of `manage_cicd.dart` and `triage_cli.dart`.
